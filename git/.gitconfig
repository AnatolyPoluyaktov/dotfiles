[core]
    excludesfile = /Users/andromeda/.gitignore_global
[user]
    name = AnatolyPoluyaktov
    email = avpoluyaktov@gmail.com
[core]
    pager = delta

[interactive]
    diffFilter = delta --color-only

[delta]
    navigate = true  # use n and N to move between diff sections
    side-by-side = true
    line-numbers = true
    hyperlinks = true
    syntax-theme = "Catppuccin Mocha"

[merge]
    conflictStyle = zdiff3
